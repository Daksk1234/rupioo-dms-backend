// File: controller/hrmFace.controller.js
import mongoose from "mongoose";
import { HrmFace } from "../model/hrmFace.model.js";
import { cleanDatabase, fail, isValidObjectId, parseArrayNumber, parseJsonArray, success, uploadedImageData } from "./_hrmCommon.js";
const buildPayload=(body,database)=>({ database, userId:body.userId, shiftId:body.shiftId||null, panNumber:body.panNumber?String(body.panNumber).trim().toUpperCase():"", nameSnapshot:body.nameSnapshot||body.name||"", salary:body.salary==null?"":String(body.salary), imageMimeType:body.imageMimeType||"image/jpeg", embedding:parseArrayNumber(body.embedding), embeddings:parseJsonArray(body.embeddings,[]), embeddingModel:body.embeddingModel||"mobile_face_net", faceQuality:body.faceQuality||{} });
export const createFace=async(req,res)=>{try{const database=cleanDatabase(req.params.database);const payload=buildPayload(req.body,database);if(!isValidObjectId(payload.userId))return fail(res,400,"Valid userId is required."); if(!payload.embedding.length&&!payload.embeddings.length)return fail(res,400,"Face embedding is required."); const existing=await HrmFace.findOne({database,userId:payload.userId,status:{$ne:"Deleted"}}); const img=await uploadedImageData(req,existing?.photoUrl||""); if(existing){Object.assign(existing,payload,{userId:new mongoose.Types.ObjectId(payload.userId),shiftId:isValidObjectId(payload.shiftId)?new mongoose.Types.ObjectId(payload.shiftId):null,status:"Active"}); if(img.photoUrl)existing.photoUrl=img.photoUrl; if(img.photoFileName)existing.photoFileName=img.photoFileName; await existing.save(); return success(res,"Face updated successfully.",existing,{updated:true});} const face=await HrmFace.create({...payload,userId:new mongoose.Types.ObjectId(payload.userId),shiftId:isValidObjectId(payload.shiftId)?new mongoose.Types.ObjectId(payload.shiftId):null,photoUrl:img.photoUrl||"",photoFileName:img.photoFileName||"",status:"Active"}); return success(res,"Face saved successfully.",face);}catch(e){return fail(res,500,"Unable to save face.",e)}};
export const listFaces=async(req,res)=>{try{const database=cleanDatabase(req.params.database);const rows=await HrmFace.find({database,status:{$ne:"Deleted"}}).sort({createdAt:-1}).lean();return success(res,"Face list fetched successfully.",rows)}catch(e){return fail(res,500,"Unable to fetch face list.",e)}};
export const getFaceById=async(req,res)=>{try{const database=cleanDatabase(req.params.database);if(!isValidObjectId(req.params.id))return fail(res,400,"Valid face id is required."); const row=await HrmFace.findOne({_id:req.params.id,database,status:{$ne:"Deleted"}}).lean(); if(!row)return fail(res,404,"Face not found."); return success(res,"Face fetched successfully.",row)}catch(e){return fail(res,500,"Unable to fetch face.",e)}};
export const updateFace=async(req,res)=>{try{const database=cleanDatabase(req.params.database);if(!isValidObjectId(req.params.id))return fail(res,400,"Valid face id is required."); const row=await HrmFace.findOne({_id:req.params.id,database,status:{$ne:"Deleted"}}); if(!row)return fail(res,404,"Face not found."); const payload=buildPayload(req.body,database); const img=await uploadedImageData(req,row.photoUrl||""); if(payload.userId&&isValidObjectId(payload.userId))row.userId=new mongoose.Types.ObjectId(payload.userId); if(payload.shiftId&&isValidObjectId(payload.shiftId))row.shiftId=new mongoose.Types.ObjectId(payload.shiftId); Object.assign(row,{...payload,database,status:"Active"}); if(img.photoUrl)row.photoUrl=img.photoUrl; if(img.photoFileName)row.photoFileName=img.photoFileName; await row.save(); return success(res,"Face updated successfully.",row)}catch(e){return fail(res,500,"Unable to update face.",e)}};
export const deleteFace=async(req,res)=>{try{const database=cleanDatabase(req.params.database);if(!isValidObjectId(req.params.id))return fail(res,400,"Valid face id is required."); const row=await HrmFace.findOne({_id:req.params.id,database,status:{$ne:"Deleted"}}); if(!row)return fail(res,404,"Face not found."); row.status="Deleted"; await row.save(); return success(res,"Face deleted successfully.",row)}catch(e){return fail(res,500,"Unable to delete face.",e)}};
